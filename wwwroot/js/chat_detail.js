function cuon() {
    document.addEventListener("DOMContentLoaded", function () {
        var messagesList = document.querySelector('.messages-list');
        messagesList.scrollTop = messagesList.scrollHeight; // Cu?n v? ?áy c?a ph?n t?
    });
}